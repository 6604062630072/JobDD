{
  "version": 2,
  "framework": "nestjs",
  "buildCommand": "prisma generate && prisma db push && tsc -p tsconfig.json",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/"
    }
  ]
}
